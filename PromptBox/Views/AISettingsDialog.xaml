<Window x:Class="PromptBox.Views.AISettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="AI Settings - API Keys" 
        Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <materialDesign:DialogHost Identifier="AISettingsDialog">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="AI Provider API Keys" 
                       FontSize="24" 
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <TextBlock Text="Configure API keys for different AI providers." 
                       FontSize="13" 
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0"/>
        </StackPanel>
        
        <!-- Provider List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProvidersPanel">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource MaterialDesignCardBackground}" 
                                CornerRadius="8" 
                                Padding="16" 
                                Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Provider Name and Status -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                             Width="24" Height="24" 
                                                             VerticalAlignment="Center"
                                                             Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                    <TextBlock Text="{Binding Name}" 
                                               FontSize="16" 
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               Margin="12,0,0,0"
                                               Foreground="{DynamicResource MaterialDesignBody}"/>
                                    <Border Background="{Binding StatusColor}" 
                                            CornerRadius="10" 
                                            Padding="8,2" 
                                            Margin="12,0,0,0"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusText}" 
                                                   FontSize="11" 
                                                   Foreground="White"/>
                                    </Border>
                                </StackPanel>
                                
                                <!-- Description -->
                                <TextBlock Grid.Row="1" Grid.Column="0" 
                                           Text="{Binding Description}" 
                                           FontSize="12"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                                           Margin="36,4,0,8"/>
                                
                                <!-- API Key Input -->
                                <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBox Grid.Column="0"
                                             Text="{Binding ApiKeyInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Enter API Key"
                                             Foreground="{DynamicResource MaterialDesignBody}"
                                             Margin="0,0,8,0"/>
                                    
                                    <Button Grid.Column="1"
                                            Content="Save"
                                            Command="{Binding SaveCommand}"
                                            Style="{StaticResource MaterialDesignRaisedButton}"
                                            Margin="0,0,8,0"
                                            Width="80"/>
                                    
                                    <Button Grid.Column="2"
                                            Content="Delete"
                                            Command="{Binding DeleteCommand}"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Foreground="#D32F2F"
                                            BorderBrush="#D32F2F"
                                            IsEnabled="{Binding HasKey}"
                                            Width="80"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusText" 
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            
            <Button Grid.Column="1"
                    Content="Close" 
                    Click="CloseButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Width="100"/>
        </Grid>
    </Grid>
    </materialDesign:DialogHost>
</Window>
